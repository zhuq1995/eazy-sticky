# 便签应用 (Sticky Notes)

基于 Vue 3 + Electron 的桌面便签应用。

## 项目简介

这是一个现代化的桌面便签应用，使用 Vue 3 Composition API 和 Electron 构建，提供流畅的用户体验和强大的功能。

### 主要特性

- 🎨 现代化的用户界面
- 📝 支持富文本编辑
- 🎯 便签拖拽和调整大小
- 💾 自动保存功能
- 🌈 自定义便签样式
- 📌 便签置顶功能
- 🔍 便签搜索和过滤

### 技术栈

- **前端框架**: Vue 3.4+ (Composition API)
- **桌面框架**: Electron 28.0+
- **构建工具**: Vite 5.0+ & electron-vite 2.0+
- **类型系统**: TypeScript 5.3+
- **状态管理**: Pinia 2.1+
- **工具库**: VueUse 10.0+

## 环境要求

- Node.js >= 18.0.0
- npm >= 9.0.0 或 pnpm >= 8.0.0

## 安装步骤

1. 克隆项目

```bash
git clone <repository-url>
cd sticky-notes-vue
```

2. 安装依赖

```bash
npm install
# 或使用 pnpm
pnpm install
```

3. 配置环境变量

```bash
cp .env.example .env
# 根据需要修改 .env 文件中的配置
```

## 开发命令

### 启动开发服务器

```bash
npm run dev
```

启动 Electron 应用的开发模式，支持热模块替换 (HMR)。

### 构建生产版本

```bash
npm run build
```

构建用于生产环境的应用程序。

### 预览构建结果

```bash
npm run preview
```

预览构建后的应用程序。

### 类型检查

```bash
npm run type-check
```

运行 TypeScript 类型检查。

### 运行测试

```bash
npm test
```

运行测试套件。

## 项目结构

```
sticky-notes-vue/
├── electron/           # Electron 主进程代码
│   ├── main.ts        # 主进程入口
│   └── preload.ts     # 预加载脚本
├── src/               # Vue 渲染进程代码
│   ├── components/    # Vue 组件
│   ├── stores/        # Pinia 状态管理
│   ├── composables/   # 组合式函数
│   ├── types/         # TypeScript 类型定义
│   ├── styles/        # 样式文件
│   ├── main.ts        # Vue 应用入口
│   └── App.vue        # 根组件
├── docs/              # 项目文档
├── .editorconfig      # 编辑器配置
├── .gitignore         # Git 忽略文件
├── .env.example       # 环境变量模板
├── package.json       # 项目配置
├── vite.config.ts     # Vite 构建配置
├── tsconfig.json      # TypeScript 配置
└── README.md          # 项目说明
```

## 开发指南

### 代码风格

项目使用 EditorConfig 统一代码风格：
- 缩进：2 个空格
- 字符编码：UTF-8
- 换行符：LF
- 文件末尾：插入空行

### 类型安全

项目启用了 TypeScript 严格模式，确保类型安全：
- 所有变量和函数都需要明确的类型声明
- 使用 `@/types` 中定义的类型接口
- 避免使用 `any` 类型

### 组件开发

- 使用 Vue 3 Composition API
- 组件文件放在 `src/components/` 目录
- 使用 TypeScript 编写组件
- 遵循单一职责原则

### 状态管理

- 使用 Pinia 进行状态管理
- Store 文件放在 `src/stores/` 目录
- 每个 Store 负责一个功能域

## 构建和部署

### 开发构建

开发构建包含 source maps 和调试信息：

```bash
npm run build:dev
```

### 生产构建

生产构建进行了优化和压缩：

```bash
npm run build
```

构建产物位于 `dist/` 目录。

## 故障排除

### 依赖安装失败

如果依赖安装失败，尝试：

```bash
# 清除缓存
npm cache clean --force
# 删除 node_modules
rm -rf node_modules
# 重新安装
npm install
```

### 端口占用

如果开发服务器端口被占用，可以在 `.env` 文件中修改端口：

```
VITE_DEV_SERVER_PORT=5174
```

### TypeScript 错误

运行类型检查查看详细错误：

```bash
npx tsc --noEmit
```

## 贡献指南

欢迎贡献代码！请遵循以下步骤：

1. Fork 项目
2. 创建特性分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 开启 Pull Request

## 许可证

[MIT License](LICENSE)

## 联系方式

如有问题或建议，请提交 Issue。

## 致谢

感谢所有开源项目的贡献者。
